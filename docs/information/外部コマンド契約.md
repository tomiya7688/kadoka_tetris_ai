# 外部コマンド契約（初期版）

1行1JSONオブジェクトで、`player`、`tick`、`sequence`、`action`を必須とする。
`action` は `move_left`、`move_right`、`rotate_cw`、`rotate_ccw`、`soft_drop`、`hard_drop`、`hold` のいずれか。
過去tick、重複sequence、不明player、不正actionは拒否する。
同一tick内はplayer番号とsequence順で適用し、処理後tickを1進める。
