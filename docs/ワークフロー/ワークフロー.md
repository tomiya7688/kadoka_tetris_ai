https://github.com/tomiya7688/kadoka_tetris_ai

kadoka
# 1.開発予定.mdの記述

codex
# 0.リモートの最新変更を取り込む
#    git fetch origin
#    git pull --ff-only origin main（ローカルに未コミット変更がある場合は先に確認）
#    取り込み後にgit statusと履歴を確認する
# 1. open Issue一覧から今回に関係するIssueだけ選び、本文を確認（Issueがない直接依頼はそれをGoalとして扱う）
# 2. 該当する開発予定・評価フィードバックだけ確認（「次へ」または予定選択時）
# 3. Goal / Required / Acceptance evidence / affected boundaryを決め、status・差分規模・対象routeを確認
# 4.ブランチを作成
# 5. routeに示されたsource・matching tests・必要な仕様だけを検索して対応・実装する。探索は受け入れ条件と検証方法が明確になったら止める
# 6. checker/静的解析や対象テストを先に実行し、共有契約・未知の影響・失敗がある場合だけ全体検証へ広げる。長いログは結果と関連行だけ記録する
# 7.ドキュメントに残す
# 8.versions.mdに記載
# 9.githubにプルリクエスト
# 10.承認
# 11.次の作業へ

評価チーム
# 1.該当部分を確認
# 2.issueに投げる


